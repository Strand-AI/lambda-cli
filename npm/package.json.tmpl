{
  "name": "lambda-mcp-${node_os}-${node_arch}",
  "version": "${node_version}",
  "description": "Platform-specific binary for lambda-mcp (${node_os}-${node_arch})",
  "os": ["${node_os}"],
  "cpu": ["${node_arch}"],
  "repository": {
    "type": "git",
    "url": "https://github.com/Strand-AI/lambda-cli"
  },
  "license": "MIT"
}
